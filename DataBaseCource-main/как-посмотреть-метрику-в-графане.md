запустить графана, слева найти explore и вставить это: 
```
rate(pg_stat_database_xact_commit{datname="marketplace"}[1m])
```

go_memstats_alloc_bytes{job="mongodb-exporter"} - чтоб байты монго смотреть